#include <iostream>
#include <fstream>
#include <vector>
#include <string>

using namespace std;

#include "handleInput.h"

int main() {
    string input = "input-small.txt";

    Matrix* matrix = createMatrix(input);
}